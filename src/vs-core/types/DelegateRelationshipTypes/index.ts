/* eslint-disable no-unused-vars */
export enum DelegateRelationshipTypes {
  COLLECTION_NAME = 'delegates',
  USER_ID_PROP_NAME = 'userId', // Owner of the vault
  COMPANY_ID_PROP_NAME = 'companyId', // Company context
  VAULT_ID_PROP_NAME = 'vaultId', // Specific vault
  DELEGATE_ID_PROP_NAME = 'delegateId', // User being granted access
}
